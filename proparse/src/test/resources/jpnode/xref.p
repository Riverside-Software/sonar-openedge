block-level on error undo, throw.
@Progress.Lang.XYZ(param1 = "abc").
find first warehouse.
for each customer where customer.country = 'france' no-lock by customer.address:

end.
find first item.

def var xxx as progress.lang.object.
xxx = cast(xxx, "Progress.Lang.Object").
xxx = new Progress.Lang.Object().

define temp-table tt1 field fld1 as char.
define temp-table tt2 field fld2 as char.
define dataset ds1 for tt1, tt2.
define new shared temp-table tt3 field fld1 as char.

function f1 returns char(x1 as Progress.Lang.Object):
  x1:toString().
end function.

procedure p1:

end procedure.

procedure p2 private:

end procedure.

publish "Hello".
subscribe "World" anywhere.
unsubscribe "World".
run foobar.p.
